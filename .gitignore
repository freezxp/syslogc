# Local tool settings
.claude/settings.local.json

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
bin/

# Built web UI embedded into the Go binary
backend/internal/api/webui/dist/*
!backend/internal/api/webui/dist/.keep

# Local Compose overrides
.env

# Deployment-specific overrides written by deploy.sh
*.local.yaml
.forwarding-enabled

# Backups written by ./deploy.sh --upgrade
backups/
